# Separate repository — lives at github.com/Hakevesc/RemAssistance
RemAssistance/

# Playwright MCP scratch output
.playwright-mcp/

# OS / editor cruft
Thumbs.db
.DS_Store

# Next.js
node_modules/
.next/
*.tsbuildinfo
.env*
# ...but the documented template is meant to be committed
!.env.example

# Dev server log
dev.log

# Browser profile that came along inside the admin UI prototype directory.
# 7.4MB of extension bundles and per-user browsing state — never a repo artifact.
Test Admin/.edge-prof/

/.next-dev*/
